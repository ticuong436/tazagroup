import { UploadFilePipe } from './upload-file.pipe';

describe('UploadFilePipe', () => {
  it('create an instance', () => {
    const pipe = new UploadFilePipe();
    expect(pipe).toBeTruthy();
  });
});
